<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.book.es.mapper.bookManger.SiteConfMapper">

    <select id="getConfByKey" resultType="java.lang.String">
        select `value` from tbl_site_conf where `key`=#{key}
    </select>

</mapper>